apiVersion: v1
kind: Service
metadata:
  name: gitops-demo
  namespace: default
spec:
  type: NodePort
  selector:
    app: gitops-demo
  ports:
    - port: 80
      targetPort: 80
      nodePort: 31000
